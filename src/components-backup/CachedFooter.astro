---
/**
 * Cached Footer Component
 * This component renders the footer once and caches the HTML for reuse across all pages
 */
import Footer from './Footer.astro';

// Cache the rendered footer HTML
let cachedFooterHTML: string | null = null;

async function getCachedFooterHTML(): Promise<string> {
  if (cachedFooterHTML === null) {
    // Import the Footer component and render it once
    // We need to capture the HTML output
    const footerComponent = await Astro.slots.render('default');
    
    // For now, we'll use a different approach - render Footer directly
    // This is a placeholder - Astro doesn't directly support this pattern
    // We'll need to refactor Footer to be a function that returns HTML
    console.log('[Footer] Generating footer HTML (will be cached for all pages)');
  }
  return cachedFooterHTML || '';
}

// For now, just render the Footer normally
// We'll need to refactor this approach
---

<Footer />